"""
Evaluation
==========
MLflow GenAI evaluation wrappers, judge metrics, and eval dataset management.

Components:
    evaluator  - mlflow.genai.evaluate() wrappers with standard judges
    metrics    - Custom judge metrics for domain-specific quality assessment
    dataset    - Eval dataset management (JSONL ↔ Delta table ↔ MLflow)
"""
