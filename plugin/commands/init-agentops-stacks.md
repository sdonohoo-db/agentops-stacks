---
description: Scaffold a new AgentOps Stacks project (DAB + CI/CD).
---

Use the `agentops-stacks` skill to scaffold a new project.

1. Collect required inputs from the user: `project_name`, `cloud`, `cicd_platform`, and `destination` (default `./<project_name>`, accepts `.` for in-place).
2. Validate and run the skill's renderer.
3. Surface the next-steps message the renderer prints.

Defer to the skill's SKILL.md for full details — including the Genie Code workspace flow, input constraints, and what the skill does NOT do.
