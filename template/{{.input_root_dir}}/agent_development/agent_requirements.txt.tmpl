# Agent dependencies are managed in pyproject.toml at the project root.
# Notebooks install via: %pip install -e ../../../
# This file is kept for backwards compatibility only.
mlflow>=3.10.0
databricks-agents>=1.9.3
