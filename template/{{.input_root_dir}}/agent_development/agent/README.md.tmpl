# Agent Creation

The `Agent.py` notebook creates the agent, logs it to MLflow, and registers the model in Unity Catalog. The agent code itself lives in `agent_server/` — this notebook handles the MLflow model lifecycle, not the agent logic.

After running `uv run setup` to install your chosen agent framework, update this notebook to match your agent's configuration (tools, model config, etc.).
