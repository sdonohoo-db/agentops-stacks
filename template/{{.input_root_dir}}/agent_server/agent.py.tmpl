"""
Agent entry point. Replace this file by running 'uv run setup' to select
and install an agent framework from databricks/app-templates.

This stub exists so the project structure is valid before setup runs.
"""

raise NotImplementedError(
    "Agent not configured. Run 'uv run setup' to select and install "
    "an agent framework from databricks/app-templates."
)
