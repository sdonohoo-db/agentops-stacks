# Vector Search

Third task in the data preparation pipeline. Creates or updates a Databricks Vector Search index from the processed data table. Uses the `vector_search_endpoint` and `vector_search_index` bundle variables.

Configuration is passed as notebook parameters from `resources/data-preparation-resource.yml`.
