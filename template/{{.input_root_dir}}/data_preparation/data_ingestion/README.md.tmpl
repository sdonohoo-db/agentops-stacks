# Data Ingestion

First task in the data preparation pipeline. Loads raw data from a source URL into a Delta table in Unity Catalog.

Configuration (catalog, schema, table name, source URL) is passed as notebook parameters from `resources/data-preparation-resource.yml`.
