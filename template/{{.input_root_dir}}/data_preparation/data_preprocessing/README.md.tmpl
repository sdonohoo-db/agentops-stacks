# Data Preprocessing

Second task in the data preparation pipeline. Reads from the raw data table, applies cleaning and chunking transformations, and writes results to a processed data table.

Configuration is passed as notebook parameters from `resources/data-preparation-resource.yml`.
